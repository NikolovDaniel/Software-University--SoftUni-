function solve(name, population, treasury) {

    const result = {
        name: name,
        population: population,
        treasury: treasury
    };


    return result;
}

const town = solve('Tortuga', 7000, 15000);

console.log(town);